---
name: Build and push Shielder-Scheduler-TEE docker image

on:
  workflow_call:
    inputs:
      ref:
        description: 'git ref: hash, branch, tag to build shielder-scheduler-tee files from'
        type: string
        required: true

jobs:
  main:
    name: Build Shielder Scheduler TEE
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout source code
        uses: actions/checkout@v4
        with:
          ref: ${{ inputs.ref }}
          fetch-depth: 0

      - name: Call action get-ref-properties
        id: get-ref-properties
        uses: Cardinal-Cryptography/github-actions/get-ref-properties@v7

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Login to Public Amazon ECR
        uses: docker/login-action@v3
        with:
          registry: ${{ vars.ECR_PUBLIC_HOST }}
          username: ${{ secrets.AWS_MAINNET_ECR_CC_ACCESS_KEY_ID }}
          password: ${{ secrets.AWS_MAINNET_ECR_CC_ACCESS_KEY }}

      - name: Build kmstool enclave CLI
        run: |
          cd crates/shielder-scheduler-tee
          chmod +x ./scripts/build_kmstool_enclave_cli.sh
          ./scripts/build_kmstool_enclave_cli.sh
          cd ../..

      - name: Build and push docker image
        id: build-image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./crates/shielder-scheduler-tee/Dockerfile
          push: true
          # yamllint disable rule:line-length
          tags: |
            ${{ vars.ECR_CC_RES_PUBLIC_REGISTRY }}shielder-scheduler:tee-${{ steps.get-ref-properties.outputs.sha }}
            ${{ github.ref == 'refs/heads/main' && format('{0}shielder-scheduler:tee-latest', vars.ECR_CC_RES_PUBLIC_REGISTRY) || '' }}
